# Este archivo permite setear la ejecucion de los tests
# con esto nos ahorramos poner todos los parametros de ejecucion en consola
[pytest]
addopts = --base-url https://www.saucedemo.com/ --headed --browser-channel chrome  --slowmo 3000 --tracing on --slowmo 3000
# si seteamos desde la consola, PISAN estos parametros